const { festTemplate, festValue, festSet, festGet } = require('./data');
const { festEach, festFor, festIf } = require('./expr');

module.exports = {
  festTemplate,
  festValue,
  festSet,
  festGet,
  festEach,
  festFor,
  festIf
};
